# import necessary packages

class ModelGeneration:
    
    def __init__(self) -> None:
        pass

    def modelChatbot(self):
        """_summary_

        Returns:
            _type_: tuple which has model, X train and Y train data
            
            *This function build the deep neural network layer and call the dataCreator() function for creating the training data.
            The process happening in this code block is mentioned below:
            1. Load training data by calling dataCreator() function
            2. build neural network layers
            3. compile the model.

        """
        try:
            " code here "
        except:
            "call log module for writing log"

    def train(self):
        """
        *This function will build a deep neural network that has specified layers mentioned in modelChatbot() function.
        The process happening in this code block is mentioned below:
        1. load the model and traing file from modelChatbot() function
        2. fit the model with appropriate callbacks and fine tune the model.

        """
        try:
            " code here "
        except:
            "call log module for writing log"
